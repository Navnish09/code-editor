{
  "judge0" : {
    "getToken" : "/submissions",
    "submission" : "/submissions/{{token}}"
  },

  "solidityCompiler" : {
    "compile" : "/compile"
  },

  "submissions" : {
    "addSubmission" : "/submission"
  }
}