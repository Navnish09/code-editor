{
  "judge0" : {
    "getToken" : "/submissions",
    "submission" : "/submissions/{{token}}"
  },

  "solidityCompiler" : {
    "compile" : "/compile"
  },

  "submissions" : {
    "addSubmission" : "/submission"
  },

  "user" : {
    "validateUser" : "/validateUser",
    "registerUser" : "/registerUser",
    "loginUser" : "/loginUser"
  },

  "questions" : {
    "getQuestion" : "/getQuestions?id={{questionId}}"
  }
}